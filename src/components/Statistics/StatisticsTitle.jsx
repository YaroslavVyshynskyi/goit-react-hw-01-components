import css from "./Statistics.module.css";
import titleSt from "./Statistics";
import PropTypes from "prop-types";

export const StatisticsTitle = (titleSt) => { 
    // if (data.title !== "") {
        return <div className={css.title_wrap}>
            <h2 className={css.title}>Upload stats</h2>
                </div>
    // }
    // return <div className={css.title_wrap}>
    //              <h2 className={css.title}>Upload stats</h2>
    //        </div>
}